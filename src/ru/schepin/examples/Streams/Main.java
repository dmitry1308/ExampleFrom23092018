package ru.schepin.examples.Streams;

public class Main {
    public static void main(String[] args) {

//        //Сумма нечетных чисел:
//        Collection<Integer> collection = new ArrayList<>();
//        for (int i = 1; i < 10; i++) {
//            collection.add(i);
//        }
//
//        Stream<Integer> stream = collection.stream();
//        Integer sumOdd = stream.filter(o -> o % 2 != 0).reduce((s1, s2) -> s1 + s2).orElse(0);
//
//        //Создание stream из значений
//        Stream<Integer> stream1 = Stream.of(5, 2, 3, 6, 9);
//
//        //Пропускает N элементов и выдает потом первый в списке
//        Integer integer = collection.stream().skip(2).findFirst().orElse(0);
//        System.out.println(integer);
//
//
//        // Добавление в список значения
//        String[] array = {"1", "5", "10"};
//        List<String> strings = Arrays.asList(array);
//        List<String> collect = strings.stream().map((s) -> s + "_1").collect(Collectors.toList());
//        System.out.println(collect);
//
//
//        //Работа с Optional: позволяет работать с null
//        Person personNew;
//        Optional<Person> person = null;
//        personNew = Objects.requireNonNull(person).orElse(new Person());
//
//
//        person.ifPresent(System.out::println);
//        Person person1 = person.orElse(new Person());




    }
}

